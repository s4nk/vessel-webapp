(function() {
    var dependencies = [
        'ngRoute',
        'ui.bootstrap',
        'vesselApp.vessels',
        'vesselApp.routes'
    ];

    var app = angular.module('vesselApp', dependencies);
})();
